If you want to run the any python file, you will need to rename it to "main". I only changed the name so its easier to identify what the code does.
Additioanlly, the TicTacToe file was created on a website called Repl.it, so the format of the Tic Tac Toe board might be scuffed if you use another console.
